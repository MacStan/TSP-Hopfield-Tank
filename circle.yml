machine:
  python:
    version: 3.6.1
dependencies:
  pre:
    - echo -ne '\n' | sudo add-apt-repository ppa:mc3man/trusty-media; sudo apt-get update; sudo apt-get install ffmpeg; locate ffmpeg
  # - sudo add-apt-repository ppa:mc3man/trusty-media;